# API Configuration
LOG_LEVEL=INFO

# LLM provider (openai or anthropic). Leave empty or "none" to use built-in generator.
LLM_PROVIDER=openai

# Generic LLM variables (preferred)
LLM_API_KEY=
LLM_MODEL=
LLM_BASE_URL=
LLM_API_VERSION=

# No provider-specific variables needed; the service uses the generic LLM_* keys above only.